/** A quiet aside where a small list would have been. */
export const Hint = ({ children }: { children: React.ReactNode }) => (
  <div style={{ color: 'var(--faint)', fontSize: 11.5, padding: '6px 0' }}>{children}</div>
);
